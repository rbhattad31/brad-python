import numpy as np

matrix = np.array([[2, 3, 6],
                   [5, 4, 4],
                   [6, 8, 6]
                   ])
print(matrix)
print("The least value in matrix is:", np.min(matrix))
print("The Highest value in matrix is:", np.max(matrix))
print("The shape of matrix is :", np.shape(matrix))
